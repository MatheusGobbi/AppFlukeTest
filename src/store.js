/*import { createStore } from 'redux';

const INITIAL_STATE = {
    pacote: null,
}

function operadora(state = INITIAL_STATE, action) {
    switch(action) {
        case '':
            return {...state,}
    }
}

export default store; */

//Futura store